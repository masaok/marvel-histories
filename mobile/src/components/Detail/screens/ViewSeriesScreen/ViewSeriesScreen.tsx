/**
 * ViewSeriesScreen
 */

import * as React from 'react';
import { Text, View } from 'react-native';

import styles from './ViewSeriesScreen.styles';

export interface Props {}

interface State {}

export default class ViewSeriesScreen extends React.Component<Props, State> {
  render() {
    return (
      <View>
        <Text>ViewSeriesScreen</Text>
      </View>
    );
  }
}
